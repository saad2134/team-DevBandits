# ================ Backend Environment Variables =============

# === 01: SCOUT AGENT (Data Scraping) ===

# SerpAPI for web scraping opportunities (uses algorithm matching if not configured)
SERP_PRIVATE_KEY = "your_serpapi_key_here"

# Firecrawl API for advanced web scraping (falls back to SerpAPI or DB if not configured)
FIRECRAWL_API_KEY = "your_firecrawl_api_key_here"



# === 04: AUDITOR AGENT (AI Features) ===
# Gemini API for AI features (uses rule-based fallback if not configured)
GEMINI_API_KEY = "your_gemini_api_key_here"