# Backend Environment Variables

# SerpAPI for web scraping opportunities (optional - uses algorithm matching if not configured)
SERP_PRIVATE_KEY = "your_serpapi_key_here"

# Gemini API for AI features (optional - uses rule-based fallback if not configured)
GEMENI_API_KEY = "your_gemini_api_key_here"